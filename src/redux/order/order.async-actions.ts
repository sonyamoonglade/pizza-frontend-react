import {AppDispatch} from "../store";
import {AxiosInstance} from "axios";


const getOrderHistory = (client: AxiosInstance) => async (dispatch: AppDispatch) => {
    const baseUrl = "http://localhost:5000/api/v1/order/"
    try{
        // implement
    }catch (e) {

    }

}